spring:
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: luc
      password: luc123456
      config:
        namespace: com-lc-dev
        prefix: auth-gateway-dev
        file-extension: yaml # nacos只支持yaml结尾的文件， 如果使用yml的话则识别不出来
      discovery:
        namespace: com-lc-dev